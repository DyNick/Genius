## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

The main problems met during the realization of the exercise was created project on the Vue js(am beginner)
The amount time passed on this task 2 and half days
I think comment bar is not user friendly( i mean button)For me much more better way is creating button with text 'Comments' and add this button to the navbarread
